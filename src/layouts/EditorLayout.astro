---
import BaseLayout from './BaseLayout.astro'
import '@puckeditor/core/dist/index.css'

interface Props {
  title?: string
}

const { title = 'Puck Editor' } = Astro.props
---

<BaseLayout title={title}>
  <slot />
</BaseLayout>
